https://github.com/zstudent/JavaFall2015_01
books:
Тимоти Бадд - object oriented programming
GoF - patterns
The Goal
Martin Fowler - refactoring

bob martin
clean code
clean coder
Angelica Langer - java generics faq
Maurice Naftalin and Philip Wadler - java generics and collections

Task:
Page 24 example

Task:
--my own 8 examples for inheritance

Task:
JavaTasksOOP.pdf
only class hierarchy
++variant #13

++modify MyList to add to the end of list

++implements MyStack with push() and pop() methods

lesson150928
--create class with 3 properties. create 3 comparators.
++BrainStates -> to outer classes

lesson150929
--questions p.1, p.2
--watch videos about designPatterns
https://www.coursera.org/course/algs4partII
http://algs4.cs.princeton.edu/home/

lesson151001
implement iterator as:
--external class
--internal class
--nested static class

--implements Stack based on array with iterator allowed to go from top to bottom
--push,pop(null if empty),iterator,top(null if empty)/peek,size,isempty.
--if stack is full - duplicate size. If count of elements < quarter of stack - decrease in 2 times stack size
--genome: print equal pairs

lesson151005
--make BoundedList(int maxSize) from MyList + implements Over and Under -flow exceptions

lesson151006
book: Brian Goetz - java concurrency in practice
book: Maurice Herlihy - multiprocessor programming
book: Gregory R. Andrews - Foundations of Multithreaded, Parallel, and Distributed Programming
++make class with static method realize deep copy of the object.method should not throws exceptions, 
it returns null instead. Write Use-class with 2 help-classes. One of them make not Serializable 
to demonstrate returning null.

lesson151008
--generate matrix in parallel. a thread to each row

lesson151012
--problem of 5 dinning chineese philosophers
sticks between ph. 
ph - thread
chobsticks - resources
10 sec eating
20 sec sleeping
if ph. can not eat in 30 seconds he died

lesson151013
--make BlockingQueue limited with size
method take send signal that queue is full
use Lock/Condition

lesson151015
--Counter - try 2 realizations. e.g. inc() for 1M times
--implement ReadWriteLock based on locks

lesson151019
--input data from keybord. scanner. analyze input string, split it, fill db table using prepareStatement. fields: name, birthday, grow, weight.
--^D - stop input. Extract from table and print data.
